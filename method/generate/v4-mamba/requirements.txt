# Match the Torch version used for the server's compiled Mamba/causal-conv1d wheels.
# Install the CUDA 13.0 variant first: see README.md. ==2.11.0 accepts +cu130.
torch==2.11.0
transformers==4.57.6
accelerate>=1.0,<2
tqdm>=4.62
